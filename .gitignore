# dependencies
/node_modules

# environment
.env*

# Google OAuth credentials and tokens — never commit
/credentials.json
/data/google-token.json

# Family context — has real PII (names, phones, medical history). Never commit.
# Commit data/family_context.example.json instead for shape documentation.
/data/family_context.json

# database
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm
*.db

# OS / editor
.DS_Store
.vscode/
.idea/

# TypeScript / build
*.tsbuildinfo
/dist

# Generated briefs — canonical record lives in the SQLite briefs/brief_items tables
/briefs
